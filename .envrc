use nix -s shell.nix -w Pipfile

export NINJA_STATUS="[Built edge %f of %t in %e sec] "
export PATH=$PATH:$(dirname $(find /nix -name run-clang-tidy.py))

PSI4_STAGED_LIB="$(pwd)/build_$(git branch-name)_ninjaclang/stage/lib"
echo "Don't forget to update PSI4_STAGED_LIB!!!"
echo "  It's currently set to $PSI4_STAGED_LIB"
export PYTHONPATH=$PSI4_STAGED_LIB:$PYTHONPATH

gtags --gtagslabel=ctags --quiet
